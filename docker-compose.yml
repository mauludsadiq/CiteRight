version: "3.8"

services:
  citeright-server:
    build: .
    ports:
      - "3000:3000"
    environment:
      - COURTLISTENER_TOKEN=${COURTLISTENER_TOKEN}
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - RUST_LOG=citeright=info
      - CITERIGHT_FIXTURE=/app/fixtures/courtlistener_fixture.json
